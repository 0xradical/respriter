NODE_ENV=development
WEBPACK_DEV_SERVER_WRITE_TO_DISK=false
GITHUB_ACCESS_TOKEN=MY_GH_ACCESS_TOKEN
# Use this if sending X-Cache-Bucket-Name locally
# and expose your credential files using docker-compose volumes
# AWS_SDK_LOAD_CONFIG=1
# AWS_CONFIG_FILE=/root/.aws/credentials
# AWS_PROFILE=mfa