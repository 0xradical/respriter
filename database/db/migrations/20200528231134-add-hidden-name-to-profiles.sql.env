BEGIN;

ALTER TABLE app.profiles ADD COLUMN _name VARCHAR;

UPDATE app.profiles SET _name = name WHERE name IS NOT NULL AND LENGTH(name) > 0;

INSERT INTO public.schema_migrations (version) VALUES ('20200528231134');

COMMIT;
