BEGIN;

ALTER TABLE app.courses ADD COLUMN up_to_date_id uuid REFERENCES app.courses(id) ON DELETE SET NULL;

CREATE INDEX index_courses_on_up_to_date_id
ON app.courses
USING btree (up_to_date_id);

INSERT INTO public.schema_migrations (version) VALUES ('20200203183213');

COMMIT;
