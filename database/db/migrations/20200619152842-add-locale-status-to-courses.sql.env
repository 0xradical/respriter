BEGIN;

CREATE TYPE app.locale_status AS ENUM (
  'empty_audio',
  'manually_overriden',
  'mismatch',
  'multiple_countries',
  'multiple_languages',
  'not_identifiable',
  'ok'
);

ALTER TABLE app.courses ADD COLUMN locale_status app.locale_status;

INSERT INTO public.schema_migrations (version) VALUES ('20200619152842');

COMMIT;
