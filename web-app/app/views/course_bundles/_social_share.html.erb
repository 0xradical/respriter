<%= render_vue_component "Sharing", {
  propsData: {
    options: {
      title: strip_tags(t(".title_html", tag: t("tags.#{@tag}"))),
      quote: strip_tags(t(".description_html", tag: t("tags.#{@tag}"))),
      description: strip_tags(t(".description_html", tag: t("tags.#{@tag}"))),
      hashtags: "classpert,online_courses,#{@tag}"
    },
    locale: I18n.locale,
  },
  locale: I18n.locale
} %>
