<%= content_for :body do %>
  <div class='l-container' style='padding-top:40px;padding-bottom:60px'>
    <h2 class='mx-Ta(c)' style='margin-bottom:40px'><%= yield :section_header %></h2>
      <%= yield %>
  </div>
<% end %>
<%= render template: 'layouts/global' %>
